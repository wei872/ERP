# 变更日志

## v5.22.0（2026-08-24）—— 中文单语 + 消息中心 + 移动底部导航 + 附件管理版

### 🌐 改为纯中文单语
- 按需求回退 v5.21 的多语言框架：移除语言切换器与英文字典，删除 `utils/i18n.ts`，
  侧边栏/登录页全部还原为中文字面量——界面更简洁，无冗余切换逻辑

### 📬 消息中心（新页面）
- 统一收件箱：聚合**审批待办 / 库存预警（逐条）/ 应收逾期 / 应付逾期 /
  待审用户 / 合同过期 / 合同临期 / 信用占用**八类消息
- 红（紧急）/ 黄（提醒）分级汇总卡可点击筛选；每条消息**一键直达处理页面**
  （复用 erp:navigate 钻取，库存预警直接带商品编码进库存表）
- 注册进导航 + 全局搜索直达

### 📱 移动端底部导航栏（App 式体验）
- <768px 显示固定底部导航：**首页 / 日报 / 消息 / 审批** + 大屏快捷入口
- 适配 iOS 安全区（safe-area-inset），主内容区自动留白不遮挡

### 📎 附件管理（单据级）
- 新表 `sys_attachment`（文件存数据库，随业务数据一并备份）+ 索引
- 四个端点：上传（5MB 限制 + 表名白名单校验）/ 列表 / 下载（原文件名 + MIME）/ 删除，均记审计日志
- **11 类单据**行操作新增「📎 附件」按钮（销售/采购/出入库/凭证/工单/报价/合同/应收应付/退货），
  弹窗内上传/预览图标/下载/删除一站完成

---

## v5.21.0（2026-08-24）—— 生产跟踪·报表工作簿·日志检索·多语言版

## v5.21.0（2026-08-24）—— 生产跟踪·报表工作簿·日志检索·多语言版

### 🏗️ 生产执行跟踪（三大跟踪集齐）
- 新端点 `/biz/production-tracking`：最近 25 张工单四段进度
  （**创建 → 领料 → 生产 → 完工**），完工进度按 完工数量÷计划数量 实时计算，
  领料量取自领料单实领合计、完工判定结合工单状态与实际产量
- 新页面「生产执行跟踪」：数量进度条（三色分级）+ 计划/完工/报废三列 + 阶段分布卡，
  注册进导航（管理员/生产/仓管）——销售/采购/生产三大执行跟踪全部就位

### 📊 财务报表 Excel 工作簿
- 新端点 `/biz/report/excel-all?period=`：**一个 Excel 文件三个 Sheet**
  （资产负债表 / 利润表 / 现金流量表），按当前公司账套生成，含汇总与明细
- 报表页新增「📊 三表工作簿」按钮，与原单表 Excel/CSV/打印并列

### 🔎 操作日志高级检索（新页面，仅管理员）
- 新端点 `/biz/audit-search`：按**操作人（模糊）/ 模块（下拉，自动聚合全部模块）/
  起止日期**组合检索 + 分页 + 总数统计
- 新页面「日志高级检索」：筛选条 + 结果表 + 分页器，安全审计与问题追溯的专用工具

### 🌐 多语言基础（i18n 框架）
- 新模块 `utils/i18n.ts`：轻量字典方案（简体中文/English），
  `t()` 取值 + `setLanguage()` 切换持久化 + 变更广播
- **顶栏语言切换器**（🇨🇳 中文 / 🇬🇧 EN），切换即时刷新
- 覆盖范围：侧边栏全部导航（业务操作 16 项 + 管理员区 5 项 + 分区标题）、
  登录页核心文案（标题/用户名/密码/登录）——业务页面文案按此框架逐步推进

---

## v5.20.0（2026-08-24）—— 采购跟踪·库存周转·待办推送·数据备份版

## v5.20.0（2026-08-24）—— 采购跟踪·库存周转·待办推送·数据备份版

### 🛒 采购执行跟踪（与销售跟踪镜像）
- 新端点 `/biz/purchase-tracking`：最近 25 张采购单四段进度
  （**下单 → 审批 → 入库 → 付款**），付款进度实时取自应付核销
- 销售/采购跟踪合并为通用组件 `OrderTrackingPage`（消除重复代码），
  采购跟踪页注册进导航（管理员/采购/会计）

### 🔄 库存周转分析（新页面）
- 新端点 `/biz/inventory-analysis`：库存金额 / 近90天出库成本 /
  **周转率（90天出库成本÷库存金额，含健康度分级）** / 可销天数 /
  **呆滞料预警**（近90天无出库记录的存货按金额 TOP5 + 处理建议）/ 出入库月度趋势
- 新页面「库存周转分析」：四张指标卡 + 出入库金额趋势图 + 呆滞料卡片，
  注册进导航（管理员/仓管/会计）

### 🔔 待办推送实时化
- 待办轮询 60 秒 → **30 秒**；待办数增加时触发**浏览器系统通知**
  （首次自动申请权限，不支持的环境静默降级）
- 铃铛角标新增**脉动动画**，有新待办时视觉上主动提醒

### 💾 数据备份一键导出
- 新端点 `/biz/backup`（管理员）：全库逻辑备份——遍历全部数据表生成
  `INSERT IGNORE` 语句的 .sql 文件（BLOB 转 HEX 字面量，外键检查包裹），恢复幂等
- 系统监控页新增「⬇ 下载数据备份」按钮 + 恢复指引；备份动作记审计日志

---

## v5.19.0（2026-08-24）—— 执行跟踪·供应商对账·导入中心·系统监控版

### 🚚 销售订单执行跟踪（新页面）
- 新端点 `/biz/sales-tracking`：最近 25 张销售单的四段执行进度
  （**下单 → 审核 → 出库 → 回款**），回款进度实时取自应收核销（百分比进度条）
- 新页面「销售执行跟踪」：四段步骤条 + 回款进度条 + 阶段分布统计卡，
  注册进导航（管理员/销售/会计）——销售内勤一屏掌握每张单走到哪一步

### 🧾 供应商对账单（与客户对账单镜像）
- 核销页「🖨️ 打印对账单」双向化：应收标签打**客户对账单**、应付标签打**供应商对账单**
- 供应商版：应付单号/日期/到期日/应付金额/已付款/未付余额 + 合计 +
  「我司对贵司未结清应付余额」确认语 + 需方/供方签章栏

### ⬆️ 数据导入中心（新页面）
- 新端点 `/biz/import-orders/{sales|purchase}`：Excel 批量导入订单
  - **同一对方名称连续多行自动合并为一张订单**
  - 商品编码存在时自动带出商品名称；数量/单价逐行校验（错误精确到行号）
  - 销售导入生成「已审核」订单 + 凭证 + 应收单；采购导入生成「待审批」订单 + 凭证 + 应付单
- 新页面「数据导入中心」：双卡片（销售/采购），模板下载 + 上传 + 结果回执
  （订单号清单 + 跳过/错误明细），注册进导航（管理员/销售/采购）

### 📡 系统运行监控（新页面，仅管理员）
- 新端点 `/biz/system-monitor`：运行时长 / 数据库延迟探活 / 注册表数 / 活动线程 /
  JVM 堆内存（用量条 + 阈值变色）/ 版本信息
- **审计统计**：今日操作数 / 近7日操作 / 今日登录成败 / 活跃用户 TOP5 / 操作模块 TOP5（条形图）
- 每 30 秒自动刷新；侧边栏管理员区新增入口

---

## v5.18.0（2026-08-24）—— 凭证模板·补货转单·对账单·图表钻取版

### 📑 凭证模板（常用分录一键调用）
- 新表 `finance_voucher_template`（模板名 + 分录行 JSON），预置 4 个常用模板
  （提现备用金 / 支付办公费用 / 股东追加投资 / 计提折旧）
- 凭证录入页新增模板条：**选择模板 → 载入分录**（借贷结构一键填充，保存前核对金额）；
  **💾 存当前为模板**（平衡校验通过才可保存，模板名唯一防重）

### ⚡ 库存预警 → 采购建议单
- 补货建议卡新增「⚡ 一键转采购建议单」：全部低于安全线的物料**按供应商自动分单**，
  生成草稿状态采购单（待审批），数量=建议补货量、单价=商品采购价、备注留痕，
  走正常采购审批流——库存预警到采购补货全自动衔接

### 🖨️ 客户对账单打印
- 核销页新增「🖨️ 打印对账单」：输入客户名即生成正式对账单预览
  （公司抬头 + 逐笔应收明细 + 合计 + 未结清余额确认语 + 双方签章栏），打印隔离直达
- 对账数据按客户全量查询（不受列表当前筛选影响）

### 🔍 仪表盘数据钻取
- **库存价值环图**：点击任一扇区 → 跳转库存表并按商品名预搜索
- **经营动态**：点击任一条目 → 按业务类型跳转对应数据表并**带上单号搜索**
  （销售→销售单、采购→采购单、凭证→凭证表、审批→审批表、库存→流水表）
- 通用表支持 `initialSearch` 入参 + 钻取时自动重挂载，钻取链路完整

---

## v5.17.0（2026-08-24）—— 多账套总账·领料追溯·销售退货·移动搜索版

### 🏢 总账按公司隔离（深度多账套）
- `CompanyContext` 请求级公司上下文：JwtFilter 从 `X-Company-Code` 头写入、请求结束清理，
  前端全局自动携带（顶栏切换账套即生效）
- **科目余额公司化**：`account_subject_balance` 增加公司维度（幂等升级 + 存量回填 + 复合索引），
  `updateBalance` / 月结 / 年结全部按当前公司读写
- **全部凭证公司化**：手工凭证 + 8 类自动联动凭证（销售/采购/核销/费用/销售成本/完工/领料/月结结转）
  均按当前账套归档；凭证列表按账套筛选
- **报表公司化**：资产负债表/利润表/净利润 KPI 按当前公司聚合——切换账套即看到该公司的账

### 🏭 生产领料批次追溯联动
- 领料确认时 **FIFO 耗用原料批次**（去向 = 工单号），领了多少、用了哪些批次全程可追溯
- 完工入库的批次成分清单**改从领料耗用记录获取**（不再重复扣减），
  无领料记录的旧流程自动回退原 BOM 展开逻辑（向后兼容）

### ↩️ 销售退货流程（全闭环）
- 已出库销售单行操作「↩ 退货」：生成退货单 + **商品回库**（按当前加权成本）+
  **双红字凭证**（冲收入 6001/1122 + 冲成本 1405/6401）+ **应收自动冲减**
- 退货数量校验（1 ~ 原明细数量）；演示数据含 2 张退货单样例；退货表注册进菜单 + 汇总面板

### 📱 移动端全局适配巡检
- 移动端新增**全屏全局搜索层**（顶栏 🔍，搜功能页 + 162 张表，大号触控目标）
- 待办铃铛面板窄屏自适应（不再溢出屏幕）

---

## v5.16.0（2026-08-24）—— 报价转订单·FIFO成本·报表打印版

### 📝 报价单 → 销售订单转换流
- 报价单列表行操作：**✓ 审批**（已报价/待审核 → 已通过）、**🔁 转订单**（已通过 → 销售订单）
- 转订单一键完成：生成销售单（已审核/未发货）+ 明细行 + 报价单置「已转订单」并留痕，
  **自动联动凭证 + 应收单**（业务财务闭环）；状态守卫防重复转换
- 演示数据 8 张报价单覆盖全状态（含报价成本结构：材料/人工/制造/管理费 + 利润率）

### 📦 批次先进先出（FIFO）成本法
- `trade_batch_trace` 增加批次入库单价：采购批次=采购价、生产批次=完工成本
- **销售出库成本改为 FIFO**：按最早批次耗用并取批次单价计价（成本更真实、可追溯）；
  批次无成本数据时自动回退加权平均价（向后兼容，不影响既有账）
- 生产批次创建同步记录成本（`createProductionBatch` 增加成本参数）

### 🖨️ 经营报表打印摘要
- 报表中心新增 🖨️ 打印：打印专用抬头（当前公司 + 期间 + 打印时间），
  复用打印隔离 CSS，整页经营分析可直接打印归档

### 🔧 质量
- SQL 占位符审计抓出并修复报价转订单明细 INSERT 占位符缺失（9 参数对 8 占位符）

---

## v5.15.0（2026-08-24）—— 账套凭证·移动审批·预算控制·看板自定义版

### 🏢 凭证按公司（账套）归档
- `voucher_main` 增加 `company_code` 维度（upgrade3 幂等，默认 HQ）
- **全部 API 请求自动携带当前公司头 `X-Company-Code`**（前端公司上下文 → 后端），
  手工凭证按当前账套归档；凭证列表新增「账套」列 + **按公司筛选**
- 说明：自动联动凭证沿用默认账套，总账按公司隔离为后续深度迭代

### 📱 移动端审批中心（新页面）
- 大卡片式待办流：类型筛选（采购/费用/请假 + 计数）、金额醒目展示、
  备注展开、**全宽大按钮一键通过/驳回**，单手完成审批
- 注册进业务导航（全角色）+ 全局搜索直达

### 💳 部门预算控制
- 新表 `oa_budget`（部门 × 月份 × 预算额，唯一键），演示数据预置 8 部门近 4 个月预算
- **费用审批超预算拦截**：提交时校验「部门当月已占用（待审批+已通过）+ 本单 > 预算」即拒绝，
  错误给出预算/已占用/本单/超出额四项数字；未设预算不限制；预算表缺失自动降级不阻断
- 工作流提交表单实时显示部门预算占用条（绿=可用，红=将超出并预警拦截）
- 新端点 `/biz/budget-usage?department=`

### 🎛️ 仪表盘自定义看板
- 「⚙️ 自定义看板」配置面板：经营动态/销售趋势/采购趋势/库存分布/近7天/部门质量
  六大板块自由显隐，**本机持久化**，一键恢复默认；行内栅格随显隐自适应

---

## v5.14.0（2026-08-24）—— 盘点·信用控制·回收站·多公司版

### 📋 库存盘点流程
- 库存页新增「库存盘点」标签：**查账面 → 录实盘 → 自动调账**三步闭环
  - 盘盈自动入库 / 盘亏自动出库（复用加权平均库存引擎，成对流水可追溯）
  - 生成盘点单（`trade_stock_check` 补商品维度列），最近盘点记录列表展示差异与金额
- 新端点 `/biz/stock-check/query` `/biz/stock-check/confirm`（仓管/会计/管理员）

### 💳 客户信用额度控制
- **销售单创建前置校验**：信用额度 >0 的客户，「应收未收 + 本单金额 > 额度」即拦截，
  错误信息给出额度/已占用/本单/超出额四项数字，指引先催收或调额度（`DataController` 事务内）
- 日报风险区新增「客户信用占用 ≥90%」预警统计

### 🗑️ 操作回收站
- 通用数据表**每次删除前自动归档**完整行数据到 `sys_deleted_backup`（JSON 快照）
- 新页面「操作回收站」：摘要列表 + 全字段预览 + **一键恢复**（新 ID 写回原表，避免主键冲突）
- 管理员可见全部，普通用户仅见本人删除记录；恢复动作记审计日志

### 🏢 多公司 / 多账套架构（基础）
- 公司主数据 `sys_company`（编码/全称/简称/税号/地址/默认标记），预置总公司 + 深圳分公司
- 顶栏**公司账套切换器**（全局上下文持久化 + 变更广播）；销售送货单/采购订单套打抬头、
  财务报表打印抬头自动使用当前公司名
- 说明：本轮交付公司主数据与全局上下文基础；按公司隔离的凭证账簿/报表为后续迭代

---

## v5.13.0（2026-08-24）—— 目标·合同·移动审批·字典维护版

### 🎯 销售目标与达成率管理
- 新表 `trade_sales_target`（月份 × 销售人员 × 目标金额，upgrade3 幂等）
- 新端点 `/biz/target-progress`：当月各销售 目标/实际/单数/达成率 + 团队合计
- **经营日报新增「销售目标达成率」板块**：团队总进度条 + 按人卡片
  （≥100% 绿 / ≥60% 蓝 / 其他黄，未设目标灰显），演示数据含 9 个月爬坡目标

### 📄 合同管理模块
- 新表 `cust_contract_main`（合同编号/名称/类型/对方/金额/签订起止/负责人/状态），
  注册进「客户供应商 > 合同档案」通用菜单，状态走字典彩签
- **合同到期预警**：日报风险区新增「已过期未完结」「30 天内到期」统计 +
  逐份清单（剩余天数红/黄分级）；`/biz/daily-report` 扩展 alerts
- 演示数据含 10 份合同：执行中/已完结/草稿/已终止 + 2 份临期 + 1 份过期未完结样例

### 📱 移动端审批快捷操作
- 工作流待办卡片直接提供 **✓ 通过 / ✕ 驳回** 一键按钮（免进详情，驳回有二次确认），
  操作区 flex-wrap 自适应，移动端单手可完成审批闭环

### 📖 数据字典可视化维护（仅管理员）
- 新页面「数据字典维护」：字典类型 → 字典项（存库值/显示名/颜色色板/排序）
  增删改 + 列级绑定管理，保存即 `refreshMeta()` 全站彩签即时生效
- 数据落 `sys_dict_item` / `sys_dict_column`（唯一键防重，友好错误提示）

---

## v5.12.0（2026-08-24）—— 套打·日报·凭证审核流·批量导入版

### 🖨️ 单据套打模板
- 销售单/采购单行操作新增「🖨️ 打印」：正式排版的**销售送货单 / 采购订单**预览弹窗
  （单据头/对手方信息/明细表/合计行/**人民币金额大写**/签字栏）
- 打印 CSS 隔离：页面存在套打弹窗时只打印该区域，一键打印归档

### 📰 经营日报（新页面）
- 新端点 `/biz/daily-report`：今日销售/采购/出入库/凭证/审批/登录（含失败）+
  本月累计 + 风险提醒（库存预警/应收逾期/应付逾期/待办审批）+ 最近销售与审批动态
- 全角色可见的日报页，支持打印日报

### 📒 凭证审核流（状态机）
- 手工凭证保存后进入「待审核」（此前直接已审核）：**待审核 → 已审核 → 已记账**
- 新端点 `/biz/voucher-audit/{no}` `/biz/voucher-post/{no}`（会计/管理员，状态守卫防跳级）
- 凭证页新增「凭证列表与审核」标签：状态彩签 + 审核/记账操作 + 搜索
- 系统自动联动凭证保持「已审核」（业务已闭环，不增加操作负担）

### ⬆️ Excel 批量导入
- 新端点 `/biz/import/{goods|customers}`：POI 解析 xlsx/xls，中文表头映射（列序无关），
  唯一键 `ON DUPLICATE KEY UPDATE` 幂等导入，返回成功/跳过/错误明细（前 5 条）
- 商品表/客户表工具栏新增「⬆ 导入」+「⬇ 模板」（一键下载带示例行的模板）

---

## v5.11.0（2026-08-24）—— 角色化审批与数据大屏版

### 🔁 审批流按角色指派
- 节点模板升级为「节点名 + 指派角色」：采购审批（采购→管理员→会计）、
  费用审批（管理员→会计）、请假审批（管理员→人事）
- **防自审自批**：与申请人同角色的节点自动跳过（记「系统」通过日志），
  全部节点被跳过时直接终结并联动业务
- 待办可见性按「用户名或所属角色」匹配（admin 全量），待办铃铛计数同步该规则
- 演示数据审批节点同步角色化（含自动跳过示例）

### 📺 数据大屏（经营驾驶舱）
- 顶栏新增大屏入口：全屏深色主题驾驶舱 —— 6 大经营 KPI、
  销售/采购月度趋势、库存价值分布、经营动态流，**实时时钟 + 60 秒自动刷新**
- Esc 或按钮一键退出；懒加载独立分包，不影响常规页面性能

---

## v5.10.0（2026-08-24）—— 毛利分析与智能补货版

### 💹 销售毛利分析（新页面）
- 新增 `/biz/profit-analysis`：以**已出库销售单**为口径实时计算
  收入（销售额）/ 成本（出库时点加权成本）/ 毛利 / 毛利率
- 三维度穿透：**月度趋势**（收入/成本/毛利组合图）、**产品毛利排行**
  （毛利额进度条 + 毛利率彩签）、**客户毛利明细表**（按毛利降序）
- 新页面「毛利分析」注册进业务导航（admin/会计/销售可见）+ 全局搜索直达

### 🧭 安全库存智能补货建议
- 新增 `/biz/replenish`：低于安全线的存货自动生成补货建议
  **建议量 = 安全线 × 2 − 现有库存 − 采购在途**（在途 = 已下单未入库明细，
  精确到 `recv_qty` 部分到货扣减），并给出该物料最近采购供应商
- 库存页新增补货建议卡片：红色缺口标识 + 在途量 + 建议补货徽章，无缺口显示绿色安心提示

---

## v5.9.0（2026-08-24）—— 多仓库管理版

### 🏬 多仓库管理
- **仓库主数据**：`trade_warehouse_main`（upgrade3 幂等），注册进通用菜单，
  演示数据预置 原料仓/成品仓/默认仓 三仓（仓管员、库区位置、启停用状态）
- **仓间调拨**：库存页新增调拨卡片（商品/源仓/目标仓/数量/事由），
  后端事务内双向行锁：源仓足额校验 + 按比例结转成本，目标仓加权平均并入，
  生成「调拨出/调拨入」成对库存流水可追溯；防超转、防同仓调拨
- **分仓库存视图**：库存看板按仓库筛选（全部/单仓），直调与调拨共用仓库下拉
- **演示数据多仓化**：期初建库/采购入库/生产领料 → 原料仓，
  生产入库/销售出库 → 成品仓；库存结存与流水按 (商品, 仓库) 维度精确勾稽

---

## v5.8.0（2026-08-24）—— 审计合规与个性化效率版

### 🕵️ 审计日志页（商用合规必备）
- 新增独立页面「审计日志」（仅管理员）：**操作日志 + 登录日志**双标签
  - 操作日志：时间/操作员/模块/操作类型（新增绿·修改黄·删除红·导出蓝）/明细/状态
  - 登录日志：时间/用户名/IP/成败状态（防爆破锁定记录可查）
- 复用汇总面板：操作按模块分布 + 今日操作数；登录按状态分布 + **今日登录失败数**
- 后端 SummaryService 新增两张审计表聚合配置，搜索/分页齐全

### ⭐ 常用表收藏
- 任意数据表标题区点 ⭐ 收藏（最多 12 个，localStorage 持久化），
  侧边栏新增「⭐ 常用收藏」分组一键直达——162 张表的系统里常用表永远在顺手的位置
- 全局搜索快捷项新增审计日志入口

---

## v5.7.0（2026-08-24）—— Excel 导出与明细钻取版

### 📊 三大财务报表 Excel(.xlsx) 导出
- 新增 `/biz/report/excel?type=&period=`：POI 生成财务归档级 Excel
  （报表抬头 + 汇总指标 + 科目明细，表头样式/列宽/数字格式齐全），仅管理员/会计可用并记审计日志
- 资产负债表导出包含资产 + 负债 + 权益全部科目行与四项合计

### 📄 单据明细一键钻取
- 新增 `DetailDrilldown` 组件：销售单/采购单/入库单/出库单/凭证/工单 6 类主单行上
  新增「📄 明细」按钮，弹窗展示全部明细行（元数据驱动中文列头 + 状态徽章 + 数字格式化）
- 复用后端搜索按关联键检索，无需新端点；凭证查看体验补齐（此前只能看通用表）

### ⌨️ 快捷操作
- 全局搜索支持快捷键「/」唤起（输入框聚焦时不误触发），占位提示同步更新

---

## v5.6.0（2026-08-24）—— 待办中心与效率工具版

### 🔔 待办中心（顶栏铃铛）
- 新增 `/biz/todos` 聚合端点：**审批待办 / 库存预警（TOP 明细）/ 应收逾期（金额）/
  应付逾期 / 待审用户（管理员）** 五类待办一次取回
- 顶栏铃铛带红色角标（99+ 封顶），60 秒自动轮询；下拉面板每类待办独立卡片，
  点击直达对应处理页面（工作流/库存表/核销/用户管理），库存在面板内直接展示缺口明细

### 📤 财务报表导出
- 三大财务报表新增「⬇ 导出 CSV」：按报表类型生成带 BOM 的 CSV
  （汇总表头 + 科目明细），Excel 直接打开无乱码可归档

### ⚙️ 通用表格效率工具
- **列显示自定义**：工具栏「⚙ 列」弹出勾选面板，隐藏不关注的列
  （如超长备注），按表持久化到 localStorage，一键恢复全部
- **Esc 关闭弹窗**：详情/编辑/业财联动/图片预览弹窗统一支持键盘关闭

---

## v5.5.0（2026-08-24）—— 易用性与财务深化版

### 🔍 全局搜索（顶栏）
- 顶栏新增**全局搜索框**：功能页 + 162 张数据表按名称/表名/子模块即搜即达，
  回车前点击结果直接跳转，Esc 清空；162 张表的系统第一次有了统一入口

### 💹 财务深化
- **应收/应付账龄分析**：核销页新增账龄分桶卡（未到期 / 逾期1-30 / 31-60 / 61-90 / 90+ 天），
  金额比例条 + 逾期合计警示，催收与付款节奏一目了然
- **三大财务报表支持打印归档**：「🖨️ 打印报表」按钮 + 全局打印样式
  （隐藏导航/操作区、自动生成打印抬头：报表名 + 会计期间 + 打印时间）
- **汇总分析面板扩展**：会计凭证页（凭证借方合计/状态分布）、生产管理页
  （计划/实际产量/工单状态分布）也自带汇总卡片与分析图

### 🖱️ 操作效率
- **仪表盘 KPI 一键穿透**：点击销售/客户/生产/净利润等指标卡直接跳转对应数据表
- **经营动态相对时间**：今天 / 昨天 / N 天前，悬停看精确日期

### 🛡️ 运维
- 新增 `deploy/backup.sh`：数据库自动备份脚本（gzip 压缩 + 按保留天数自动清理），
  附 crontab 配置示例

---

## v5.4.0（2026-08-24）—— 体验统一与信息密度增强版

### 🧹 剩余已知项全部清零
- **全局通知系统**：新增 `ToastHost` + `toastNotify`（按文案自动判定成功/失败/警告/信息类型，
  顶部居中堆叠、入场动画、点击关闭）；10 个页面各自的本地 Toast 全部迁移统一，消除实现碎片
- **必填字段标识与校验**：通用表单按元数据 `nullable` 给必填列加红星；保存前拦截空必填项，
  提示「必填字段不能为空：X、Y」，不再把晦涩的数据库报错丢给用户
- **移动端适配**：<768px 时侧边栏变为抽屉式导航（汉堡按钮 + 遮罩 + 选中自动收起），
  头部高度/边距响应式收缩，页面标题自动截断

### 📈 数据信息密度增强
- **仪表盘新增「⚡ 经营动态」**：聚合最近的销售/采购/凭证/审批/库存事件
  （新端点 `/biz/activity-feed`），业务脉搏一屏可见
- **KPI 卡增加本月增量**：累计销售/累计采购卡片附「本月新增 ¥X」，净利润卡标注结转来源
- **工作流页新增待办概览**：待办数 / 待审金额合计 / 按审批类型分布统计卡

### 🎨 视觉降噪收尾
- 其余 8 个页面（凭证/报表/库存/生产/模板库/核销/用户管理/RBAC）的深色引导卡
  全部折叠化，与仪表盘/报表/通用表保持一致：默认一行，点击展开

---

## v5.3.1（2026-08-24）—— 界面体验修复版

针对界面审查发现的不足，逐项修复：

### 交互逻辑缺陷
- **会话失效不再是"假登录态"**：401 时广播全局事件，自动退出登录并在登录页提示
  「会话已过期，请重新登录」（此前 Token 过期后页面停留在登录态、所有请求静默失败）
- **退出登录增加二次确认**（内联确认条，4 秒自动还原），防止误触
- **通用数据表支持列排序**：点击表头按该列服务端排序（升降序切换，列名白名单校验防注入），
  162 张表全部可用

### 视觉降噪
- 控制台/报表中心/通用数据表三处大篇幅「使用指南」深色卡片改为**默认收起的可折叠条**，
  首屏还给业务数据，需要时一键展开
- 表格长文本列（备注等）截断显示 + 悬停查看完整内容，避免超宽行撑破版式

### 登录页完善
- 新增**演示账号一键填充**（管理员/销售/会计/仓管/生产 5 个快捷入口，便于评估）
- 密码框增加**显示/隐藏切换**、补全 autocomplete 属性
- 注册密码提示与后端策略对齐（至少 8 位含字母和数字）

### 稳健性
- 移除 Google Fonts 远程字体引入（内网/商用部署无外网时是渲染阻塞源），
  改为系统字体栈回退（本机装有 Inter 时自动生效）
- 小屏（<768px）进入时侧边栏自动折叠为图标模式，给数据区让出空间

### 已知待改进项（后续版本）
- 各页面 Toast 为各自实现，尚未统一为全局通知组件
- 移动端为响应式收缩而非专门的手势交互
- 表单弹窗未标注必填字段星号

---

## v5.3.0（2026-08-24）—— 业财一体化深化版

### 🔗 业财一体化（业务流与财务流实时联动，且联动可见）
- **一键入库/出库/审批/核销/领料/月结** 全部在事务内同步生成凭证、应收应付、库存流水、批次记录（既有能力）
- **新增「🔗 业财」联动全景**：销售单/采购单列表行按钮，一屏展示该单据自动联动的
  会计凭证（含借贷分录明细）、应收/应付单及余额、出入库单、关联批次 —— 业财勾稽关系直观可查
- 新增端点 `GET /biz/doc-links/{单号}`

### 📊 每个业务列表页自带汇总卡片 + 分析图
- 新增 `SummaryService`：20 张核心业务表内置聚合配置（金额合计/记录数/预警数 + 近 6 个月趋势 + TOP8 分布）
- 通用数据表页顶部新增**数据汇总面板**：汇总卡片 + 「📈 生成分析图」按钮（趋势面积图 / 分布环形图一键切换）
- 新增端点 `GET /data/{table}/summary`（聚合列全部服务端内置配置，无注入面）

### 🧬 批次追溯（开箱即用）
- 新增 `trade_batch_trace` 批次台账 + `trade_batch_consume` 耗用记录（`upgrade3.sql` 幂等）
- **采购入库自动建批次**（PB-采购单-行号）；**生产入库生成生产批次**（MB-工单）并回写
  FIFO 耗用的原料批次成分；**销售出库 FIFO 耗用成品批次**并记录流向
- 库存页新增「🧬 批次追溯」标签页：输入批次号**正向追溯**（批次 → 工单/客户），
  输入销售单号**反向追溯**（销售单 → 成品批次 → 原料批次 → 采购单/供应商，一键召回定位），
  图谱节点可点击跳转继续追溯
- 演示数据集内置完整批次仿真（采购批次/领料耗用/生产成分/销售流向与出入库逐笔同步）

### 🏭 行业物料编码规则（电子智造，开箱即用）
- 新增 `sys_code_rule` 编码规则表，预置 12 条行业规则：
  `FG-`成品 / `IC-`芯片 / `MOD-`模组 / `PCB-`电路板 / `PWR-`电源 / `ENC-`结构件 /
  `DSP-`显示 / `CON-`连接件 / `SEN-`传感器 / `ISO-`隔离器件 / `PKG-`包装 / `FST-`紧固件
- 商品表新增页支持 **⚡ 一键生成编码**（选择分类规则 → 扫描现有流水号自动+1，天然防重）
- 演示物料全部按行业规则编码（IC-0001 主控芯片、MOD-0001 4G模组…），BOM/单据联动一致
- 新增端点 `GET /biz/code-rules`、`GET /biz/next-code/{规则}`

---

## v5.2.0（2026-08-24）—— 功能完善与数据丰满版

### 🎁 演示数据集（开箱即有清晰直观的数据）
- **新增 9 个月跨度、全链路自洽的演示业务数据**（`scripts/generate_demo_data.mjs` 生成 → `demo_data.sql`，2400+ 行）：
  - 16 个商品（3 成品 + 13 物料）· 8 客户 · 6 供应商 · 3 组成品 BOM
  - 61 张销售单 / 36 张采购单（按月分布，近期增长趋势）· 12 张生产工单（领料/入库/报废/成本结算全链路）
  - 286 张会计凭证（销售/采购/成本结转/领料/完工/回款/付款/工资/月结结转）
  - 库存结存与 285 条流水按加权平均成本精确勾稽；应收应付与核销一一对应
  - 24 名员工 × 3 个月工资明细 · 质检 · 发货签收 · 4 条待办审批 + 4 条历史审批
- **勾稽一致性**：凭证借贷平衡、科目余额按后端同款公式滚存、已结账月份损益结转 4104
  → 资产负债表平衡、利润表/现金流量表按月有数、仪表盘全部趋势图有鲜活曲线
- **日期相对当前时间生成**（`DATE_SUB(CURDATE(), ...)`）：任何时候部署都呈现最近 9 个月的数据
- **装载开关**：`SEED_DEMO_DATA`（默认 true）；仅在业务表为空时装载、全量 `INSERT IGNORE`，
  永不覆盖真实数据；Docker 首次初始化与后端 `DemoDataRunner` 双通道（已部署实例重启即可补数据）

### 🔧 逻辑严谨性修复（消除运行时报错与数据断裂）
- **修复一键入库/出库 SQL 列错误**：原实现引用不存在的列（`ref_purchase_no`/`ref_sales_no`/
  `supplier_name`/`customer_name`/`total_amount`），实际执行必然报错；现按真实表结构
  （`in_type`/`out_type`/`ref_no`/`amount`/`line_no`）落库
- **BOM 数据模型补全**：`prod_bom_structure` 新增 `parent_code`/`component_code` 父子件列
  （`upgrade3.sql` 幂等升级）；MRP 净需求、BOM 成本滚算、工单领料展算改为按父件展开、
  取真实子件，兼容旧数据回退查询；物料名称取商品主数据而非编码
- **修复 MRP 一键转采购**：不再引用商品表不存在的供应商列，改为复用该物料最近采购供应商；
  采购单状态/列与字典和表结构对齐
- **生产成本结算**：工序成本按 BOM 子件匹配工序（修复原自引用子查询），补充按产量分摊的人工制费
- **生产入库流水**关联入库单号，库存日志可追溯
- **创建工单**：商品名称取商品主数据；工单初始状态用字典值「进行中」

### 🗃️ 数据模型补丁（database/upgrade3.sql，幂等）
- `trade_sales_main` + `warehouse` / `shipping_status`（销售出库联动依赖）
- `trade_purchase_main` + `warehouse` / `arrival_status`（采购入库联动依赖）
- `trade_purchase_detail` + `recv_qty`（MRP 在途量精确计算）
- `prod_bom_structure` + `parent_code` / `component_code` + 索引
- 字典补齐 24 个状态值（未发货/运输中/已签收/部分核销/待审批…）与 15 张表的列级字典绑定
  → 全部种子/业务状态在通用表格中呈现彩色标签

### 🖥️ 数据呈现优化
- 通用数据表：数字列右对齐 + 千分位 + 最多 2 位小数；日期列只显示日期；空值统一「—」占位

### 📖 文档
- 手工部署数据库步骤更新为 5 个脚本的完整顺序；部署指南补充演示数据说明

---

## v5.1.0（2026-08-24）—— 商用加固版

### 🔐 安全加固
- **登录防暴力破解**：同一「用户名 + IP」连续失败 5 次锁定 15 分钟（`AuthController`）。
- **注册密码策略**：至少 8 位且同时包含字母与数字；用户名限 3-32 位字母/数字/下划线。
- **修复管理员密码被重置漏洞**：重启播种逻辑只创建不存在的账号，绝不覆盖已有密码/资料（此前每次重启都会把改过的 admin 密码重置回 admin123）。
- **JWT 生产强校验**：`APP_ENV=prod` 时使用内置默认密钥直接拒绝启动；密钥长度不足 32 字节同样拒绝启动；Token 有效期可用 `JWT_EXPIRATION` 配置。
- **CORS 白名单化**：不再允许 `*` + 凭证的危险组合，仅放行 `CORS_ALLOWED_ORIGINS` 中列出的 Origin。
- **API 安全响应头**：`X-Content-Type-Options: nosniff`、`X-Frame-Options: SAMEORIGIN`、`Referrer-Policy`、API 响应 `Cache-Control: no-store`。
- **全局异常兜底**：新增 `GlobalExceptionHandler`，统一错误格式，杜绝堆栈/SQL 细节泄露；登录异常信息不再回显内部错误。

### 🚀 部署与运维
- **新增 `docker-compose.yml`**：MySQL 8 + 后端 + 前端三容器一键部署，含健康检查、数据卷持久化、首次自动执行全部 4 个初始化 SQL。
- **后端 Dockerfile 多阶段构建**：镜像内完成 Maven 打包，运行镜像改为 JRE + 非 root 用户 + 健康检查。
- **新增 `.env.example`**：集中声明所有部署环境变量。
- **配置全面环境变量化**：数据库连接、连接池、JWT、日志级别等均支持环境变量注入，代码库不再携带任何真实凭据。
- **健康检查端点** `GET /health`：返回应用状态 + 数据库连通性，供容器探活/负载均衡使用。
- **nginx 加固**：gzip、安全响应头、静态资源长缓存、`client_max_body_size` 对齐后端。

### 📦 仓库与前端
- **移除误提交的构建产物**：`frontend/node_modules`（7800+ 文件）与 `frontend/dist` 不再纳入版本控制，`.gitignore` 补齐。
- **路由级代码分割**：12 个业务页面改为 `React.lazy` 懒加载，登录后首屏 JS 从 ~593KB 降至 ~41KB（gzip 后首屏约 75KB）。
- **API 基址可配置**：`VITE_API_BASE` 支持前后端分离部署。
- 注册页密码提示与后端策略对齐（8 位 + 字母 + 数字）。

### 📖 文档
- 新增 `docs/DEPLOYMENT.md`：商用部署指南（Docker/手工/安全检查清单/备份恢复）。
- README 更新生产部署与环境变量说明。

---

## v5.0.0 —— 首个全功能版本

- 13 个业务页面、60 个 REST 端点、162 张业务表全链路打通。
- 工作流审批、会计凭证、三大财务报表、应收应付核销、生产全生命周期、MRP、期末结账、财务模板库、用户管理与 RBAC。
